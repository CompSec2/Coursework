version: '2.2'

services:
  tcpdump:
    image: kaazing/tcpdump
    network_mode: "host"
    volumes:
      - ./tcpdump:/tcpdump
  bind:
    restart: always
    build: ./dns
    ports:
      - "10000:10000/tcp"
    volumes:
      - /data
    networks:
      net:
        ipv4_address: 172.16.238.8
    environment:
      - ROOT_PASSWORD=highentropy
  attacker:
    build:
      context: ./attacker
    networks:
      - net
    volumes:
      - ./attacker/attack.py:/attack.py:ro
  victim:
    container_name: coursework_victim
    build: 
      context: ./victim
    ports:  
      - "5000:5000"
    networks:
      net:
        ipv4_address: 172.16.238.10
    volumes:
      - ./victim/sniff.py:/sniff.py:ro
    working_dir: /var/www/
    extra_hosts:
      - "www.victim.com:127.0.0.1"
    cpus: "1"
    cpuset: "0"
    mem_limit: "512M"


networks:
  net:
    ipam:
      driver: default
      config:
        - subnet: "172.16.238.0/24"
